# Notes for Claude

- Do not create a separate feature/working branch for changes in this repo.
  Work directly on `main` and push there, unless the user explicitly asks
  for a branch (or a PR) in a given task.
